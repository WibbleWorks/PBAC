# PBAC Phase 2 Improvements (implemented 2026-09-19)

Follow-up to `2026-09-19-pbac-improvement.md` (merged as PR #1).
All items below implemented on merged main, gates green.

## Implemented

**Policy Lab fidelity (`pbac-animations.js`)**
- Rego: per-scenario `teamId` + URL→group derivation evaluated live; 5th
  scenario (mallory, Team123 PUT /teams/456) shows the IDOR-closing
  derivation miss → deny.
- Cedar: 5th scenario (owner unknown) demonstrates the missing-attribute
  error → policy skipped → default deny path (Lesson 8 Q6).
- OpenFGA: `fgaCheck()` computes check() from tuples + model (concentric
  viewer←editor, transitive team membership); canvas renders the computed
  value, `s.result` retained as documented expectation with drift warning.
  Verified: 3 documented + 2 adversarial cases pass.

**Capstone rubrics (L23/L24)**
- Partial = 1 pt every row (incl. 2-pt rows); max 20, pass 14 — stated.
- L23 row 5: n≥100, warm+cold split, named tool, fail-closed timeout.
- L23 M3 scoped to exactly one enforcement pattern.
- L24: OSS alternate track (L12 compile-to-SQL + Postgres RLS, no tenant).

**Test gaps**
- `tests/smoke.mjs`: browser path answers via optionOrder inversion,
  asserts 100%, exercises showReview; static live-region contract asserts.
- `scripts/validate-lessons.mjs`: semantic block (single-correct,
  non-empty explanation, concept ∈ concepts, sampleSize==5,
  passingScore ladder, prereq resolution).
- `scripts/extract-lessons.mjs` + validator: `unlocked` carried/compared
  (absent ≡ false), no silent drift.
- `tests/a11y.mjs`: results + capstone-table audit, live-region DOM assert.

**Structure (Option B, mechanical half)**
- True-dep prereqs: L11←rego_api_authz, L13←cedar_foundations,
  L14←openfga_foundations, L15←opa_production, L16←rego_foundations,
  L18←conftest_ci. DAG verified acyclic, 24 lessons, no dangles.
- L11↔L19 cross-pointers (setup vs choice).

**C-full content (one targeted addition per lesson)**
- L10: XACML XML decoder-ring sample; ReBAC-lineage claim qualified.
- L11: hot-reload caveat; bundle signing + /v1/status + log masking.
- L13: schema-limits note; permit-template + link/unlink lifecycle;
  IsAuthorized JSON + CLI pin note.
- L14: condition+context DSL sample; list-objects bounding in content.
- L15: audit vs enforce; sync replication; namespaceSelector + fail policy.
- L16: runnable layout commands; exception scoping by namespace+labels.
- L19: rest-rego numbers caveated; ext_authz + RULES minimal shapes.
- L20: *_test.rego sample; measurable deny-coverage gate; diff command.
- L21: inventory grep sketch; PII allowlist; numeric exit example.
- L22: adversarial set minimum; OPA/Cedar tool-gating alternative.

## Deferred (deliberate, with rationale)

- **Mass re-timing of d4 lessons**: no completion-rate data; guessing
  numbers is worse than honest uniform ones. Revisit with learner data.
- **Quiz difficulty quota (≥2 L3/bank) + distractor-plausibility lint**:
  needs discrimination data first; current banks pass validity gates.
- **L18 split in two**: content is dense but coherent; split on demand.
- **Full browser smoke + axe runs**: need Playwright + Chrome env
  (`npx playwright install chromium && node tests/smoke.mjs`,
  `node tests/a11y.mjs`).

## Gates (all green at write time)

`node --check` ×4 · validate-lessons (schema + semantic + mirror) ·
check-concept-coverage · lint-version-pinning · smoke (static; browser
skipped — no Playwright in env).
